# 贡献说明

1. 创建独立分支，使用通用机制修复，避免按某个种子/坐标写演示特判。
2. 运行`python -m unittest discover -s tests -v`及离线demo。
3. 说明修改的职责层、可执行约束和测试边界；实机结果注明版本、种子、干预与失败。
4. 不提交密钥、私人配置、存档、原始大型日志或录像。需要fixture时只保留诊断所需的结构化状态。
5. PR中区分模型返回、字段采用、动作变化与实际收益。
